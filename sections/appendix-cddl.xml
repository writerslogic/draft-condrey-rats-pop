<section anchor="sec-appendix-cddl" title="CDDL Schema">

  <t>This appendix defines the CDDL schema for the core data
  structures used in the Proof of Process protocol.</t>

  <figure>
    <artwork type="cddl"><![CDATA[
evidence-packet = {
  version: uint,
  session-id: uuid,
  sequence: uint,
  timestamp: tdate,
  prev-hash: bstr,
  content-hash: bstr,
  metrics: process-metrics,
  vdf-proof: vdf-output,
  seal: bstr,
}

process-metrics = {
  cadence-mean: float,
  cadence-variance: float,
  entropy: float,
  revision-count: uint,
  pause-distribution: [* float],
  ? error-rate: float,
}

vdf-output = {
  input: bstr,
  output: bstr,
  proof: bstr,
  difficulty: uint,
}

uuid = bstr .size 16
    ]]></artwork>
  </figure>

</section>
